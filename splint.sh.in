#!/bin/env sh


if [ "x@SPLINT@" = "xSPLINT-NOTFOUND" ]; then
    echo SKIP!
    exit 0
    #exit 77
fi

find ${SRCDIR} -type f \
     \( -name  \*.c \) \
     -exec ${SPLINT} \
     -checks \
     -exportlocal \
     +matchanyintegral \
     {} \;
exit $?
